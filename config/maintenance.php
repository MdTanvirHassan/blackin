<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Force Maintenance Mode
    |--------------------------------------------------------------------------
    |
    | When true, every URL shows the maintenance page (including admin/login).
    | This is committed with the code, so it also applies after you push to live.
    | Set to false and deploy again when the site should reopen.
    |
    */

    'enabled' => true,

];
